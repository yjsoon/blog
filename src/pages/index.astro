---
import { getCollection } from "astro:content";
import Layout from "@/layouts/Layout.astro";
import Header from "@/components/Header.astro";
import Footer from "@/components/Footer.astro";
import Socials from "@/components/Socials.astro";
import LinkButton from "@/components/LinkButton.astro";
import FullCard from "@/components/FullCard.astro";
import HeadlineCard from "@/components/HeadlineCard.astro";
import Hr from "@/components/Hr.astro";
import getSortedPosts from "@/utils/getSortedPosts";
import IconArrowRight from "@/assets/icons/IconArrowRight.svg";
import { SOCIALS } from "@/constants";
import Logo from "@/assets/logo.png";

const posts = await getCollection("blog");

const sortedPosts = getSortedPosts(posts);
const featuredPosts = sortedPosts.filter(({ data }) => data.featured);
const recentPosts = sortedPosts.filter(({ data }) => !data.featured);

// Split recent posts into full posts (first 5) and headlines (next 5)
const fullPosts = recentPosts.slice(0, 5);
const headlinePosts = recentPosts.slice(5, 10);
---

<Layout>
  <Header />
  <main id="main-content" data-layout="index">
    <section id="hero" class="py-8">
      <div class="flex items-center gap-4 mb-6">
        <div>
          <h1 class="text-3xl font-bold sm:text-4xl py-2">yjsoon</h1>
          <p class="text-foreground/70">I write about tech, programming, teaching, design, running a company, 
          and other amusing things. <a href="/about" class="link">More here</a>.</p>
        {
          // only display if at least one social link is enabled
          SOCIALS.length > 0 && (
            <div class="mt-3 flex flex-wrap items-center gap-2">
              <!-- <span class="text-sm text-foreground/70">Find me:</span> -->
              <Socials />
            </div>
          )
        }
        </div>
        <img src={Logo.src} alt="yjsoon" class="w-32 h-32 rounded-full" />
      </div>

    </section>

    <Hr />

    {
      featuredPosts.length > 0 && (
        <>
          <section id="featured" class="pt-12 pb-6">
            <h2 class="text-2xl font-semibold tracking-wide">Featured</h2>
            <ul>
              {featuredPosts.map(data => (
                <FullCard variant="h3" {...data} />
              ))}
            </ul>
          </section>
          {recentPosts.length > 0 && <Hr />}
        </>
      )
    }

    {
      fullPosts.length > 0 && (
        <section id="recent-posts" class="pt-6 pb-6">
          <ul>
            {fullPosts.map(data => <FullCard variant="h3" {...data} />)}
          </ul>
        </section>
      )
    }

    {
      headlinePosts.length > 0 && (
        <>
          <Hr />
          <section id="more-posts" class="pt-6 pb-6">
            <h2 class="text-xl font-semibold mb-6">Further back in time</h2>
            <ul class="ml-2">
              {headlinePosts.map(data => <HeadlineCard {...data} />)}
            </ul>
          </section>
        </>
      )
    }

    <div class="my-8 text-center">
      <LinkButton href="/posts/">
        All Posts
        <IconArrowRight class="inline-block rtl:-rotate-180" />
      </LinkButton>
    </div>
  </main>
  <Footer />
</Layout>

<script>
  document.addEventListener("astro:page-load", () => {
    const indexLayout = (document.querySelector("#main-content") as HTMLElement)
      ?.dataset?.layout;
    if (indexLayout) {
      sessionStorage.setItem("backUrl", "/");
    }
  });
</script>
